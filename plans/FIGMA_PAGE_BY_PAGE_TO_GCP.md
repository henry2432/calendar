# Figma → Roo Code → GCP 一页一页部署策略

## 🎯 核心流程

```
Figma Design (一页一页)
    ↓
Roo Code 自动生成 React + TypeScript
    ↓
部署到 GCP
    ↓
下一页...（重复）
```

**优势**：
- ✅ 每页完成即可部署，快速反馈
- ✅ 减少复杂性（无需 Make.com）
- ✅ 更灵活的调整空间
- ✅ Header/Footer 一次性复用

---

## 📋 准备阶段（第 1-2 天）

### 步骤 1：Header/Footer 设计和生成

#### 1.1 在 Figma 中完成 Header/Footer

**Header 组件**：
```
┌─────────────────────────────────────┐
│ Logo | Nav Links | Auth Button      │
├─────────────────────────────────────┤
│ 首頁 | 租借服務 | 水上活動 | 品牌商店 │
│ 關於 | Blog | 活動策劃 | 聯絡       │
└─────────────────────────────────────┘
```

**Footer 组件**：
```
┌─────────────────────────────────────┐
│ Company Info | Quick Links | Social  │
├─────────────────────────────────────┤
│ © 2026 Kayarine | Privacy | Terms   │
└─────────────────────────────────────┘
```

**Design Checklist**：
- [ ] Logo 设计完成
- [ ] Navigation 菜单布局
- [ ] Auth 按钮样式
- [ ] 响应式设计（Desktop/Tablet/Mobile）
- [ ] 颜色和字体定义清晰

#### 1.2 我生成 Header/Footer 组件

**文件输出**：
```
src/components/
├── Header.tsx (可复用共享组件)
├── Footer.tsx (可复用共享组件)
└── Layout.tsx (包含 Header + Footer 的布局模板)
```

**Layout 模板**：
```typescript
// src/components/Layout.tsx
export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <Header />
      <main>{children}</main>
      <Footer />
    </>
  )
}
```

**所有页面共用**：
```typescript
// src/app/[page]/page.tsx
import Layout from '@/components/Layout'

export default function Page() {
  return (
    <Layout>
      {/* 页面特定内容 */}
    </Layout>
  )
}
```

---

## 🚀 循环部署流程（第 3+ 天）

### 循环 1-11：每页重复以下步骤

```
Loop Page 1-11:
  ↓
1. Figma 设计这一页
  ↓
2. 我生成这一页的 React 组件
  ↓
3. 集成到共享的 Layout
  ↓
4. 部署到 GCP
  ↓
5. 验证上线
  ↓
Loop Next Page
```

---

## 📄 每页的执行流程

### 示例：第 1 页 - 首頁 (Homepage)

#### 第 1 步：Figma 设计（2-3 小时）

**您在 Figma 中做**：
```
首頁设计包括：
├─ Hero Section
│  ├─ 大背景图
│  ├─ 标题文案
│  ├─ 副标题
│  └─ CTA 按钮
├─ Featured Tours (3-4 个)
├─ Why Choose Us (特性列表)
├─ Testimonials (评价)
├─ Latest Blog Posts
└─ CTA Section
```

**设计文件标准**：
- 清晰标记所有组件
- 确定颜色和尺寸
- 标注字体大小
- 提供内容占位符

#### 第 2 步：我生成代码（1-2 小时）

**我做的事**：
1. 查看您的 Figma 设计
2. 生成完整的 React 组件
3. 应用 Tailwind CSS 样式
4. 添加 TypeScript 类型
5. 优化响应式设计

**生成的文件**：
```
src/app/
├── page.tsx (首页)
├── components/
│   ├── HeroSection.tsx
│   ├── FeaturedTours.tsx
│   ├── WhyChooseUs.tsx
│   ├── Testimonials.tsx
│   └── LatestBlogPosts.tsx
└── lib/
    └── constants.ts (内容数据)
```

**代码示例**：
```typescript
// src/app/page.tsx
import Layout from '@/components/Layout'
import HeroSection from '@/app/components/HeroSection'
import FeaturedTours from '@/app/components/FeaturedTours'

export const metadata = {
  title: '首頁 | Kayarine',
  description: '...'
}

export default function HomePage() {
  return (
    <Layout>
      <HeroSection />
      <FeaturedTours />
      {/* ... 其他组件 */}
    </Layout>
  )
}
```

#### 第 3 步：本地测试和调整（1 小时）

```
✓ 运行 npm run dev
✓ 检查布局和样式
✓ 测试响应式设计（Desktop/Tablet/Mobile）
✓ 检查性能（Lighthouse）
✓ 修复任何问题
```

#### 第 4 步：部署到 GCP（30 分钟）

**部署流程**：
```bash
# 1. 提交代码到 GitHub
git add .
git commit -m "feat: Add homepage static page"
git push origin main

# 2. GitHub Actions 自动运行（在 GCP 部署）
# 触发器：.github/workflows/deploy.yml
# 结果：自动部署到 GCP Cloud Run

# 3. 验证上线
curl https://kayarine.gcp-domain.com
```

**GCP 部署配置**（一次性设置）：
```yaml
# .github/workflows/deploy.yml
name: Deploy to GCP

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Build
        run: npm ci && npm run build
      
      - name: Deploy to GCP
        run: |
          gcloud run deploy kayarine-frontend \
            --source . \
            --platform managed \
            --region asia-east1 \
            --allow-unauthenticated
```

#### 第 5 步：验证（15 分钟）

```
✓ 打开 GCP 上的网址
✓ 检查页面显示是否正确
✓ 点击所有链接验证导航
✓ 在移动设备上测试
✓ 检查控制台无错误
✓ 记录完成
```

**完成标记**：首页 ✅ (总 1/11)

---

## 📊 完整的 11 页计划

### 第 1 周（前 5 页）

| 页面 | Figma | 代码生成 | 部署 | 验证 | 状态 |
|------|-------|---------|------|------|------|
| **1. 首頁** | ✅ | ⏳ | ⏳ | ⏳ | 进行中 |
| **2. 租借服務** | ⏳ | | | | 待开始 |
| **3. 水上活動** | | | | | 待开始 |
| **4. 品牌商店** | | | | | 待开始 |
| **5. 關於我們** | | | | | 待开始 |

### 第 2 周（后 6 页）

| 页面 | Figma | 代码生成 | 部署 | 验证 | 状态 |
|------|-------|---------|------|------|------|
| **6. Blog** | | | | | 待开始 |
| **7. 私隱政策** | | | | | 待开始 |
| **8. 旅程政策** | | | | | 待开始 |
| **9. 預訂及取消** | | | | | 待开始 |
| **10. 條款及細則** | | | | | 待开始 |
| **11. 活動策劃** | | | | | 待开始 |

---

## ⏱️ 时间预估

### 每页的时间投入

```
Figma 设计：2-3 小时 (您完成)
Roo 代码生成：1-2 小时 (我完成)
测试和调整：1 小时 (我完成)
部署和验证：1 小时 (自动化 + 验证)
─────────────────────────
总计：5-7 小时 / 页

11 页 × 6 小时平均 = 66 小时
分散到 2 周 = 每周 33 小时 (约 6-7 小时/天)
```

**加速方案**：
- 多个页面并行（Figma 设计同时进行）
- 相似页面快速复用（政策页面）
- 我可以提前生成代码

---

## 📁 GCP 部署结构

### 一次性配置

```
kayarine-frontend/
├── .github/
│   └── workflows/
│       └── deploy.yml (部署自动化)
├── src/
│   ├── app/
│   │   ├── page.tsx (首页)
│   │   ├── rental/page.tsx
│   │   ├── services/page.tsx
│   │   └── ... (其他页面)
│   ├── components/
│   │   ├── Header.tsx (共享)
│   │   ├── Footer.tsx (共享)
│   │   ├── Layout.tsx (共享)
│   │   └── (页面特定组件)
│   └── lib/
├── public/
├── next.config.js
└── Dockerfile (GCP 部署)

Dockerfile:
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

---

## 🔄 工作流总结

### 您的工作
```
每页：在 Figma 中做设计（2-3 小时）
     告诉我准备好了
     等待我生成代码
     验证效果
     告诉我进行下一页
```

### 我的工作
```
每页：您 Figma 设计好后
     我生成完整的 React 组件（1-2 小时）
     提交到 GitHub
     GitHub Actions 自动部署到 GCP
     验证上线
```

### 自动化
```
每页：代码 push 到 GitHub main
     ↓
     GitHub Actions 自动触发
     ↓
     Next.js 构建
     ↓
     Docker 镜像打包
     ↓
     自动部署到 GCP Cloud Run
     ↓
     上线完成
```

---

## 🚀 立即开始

### 第 0 天：准备

- [ ] 我生成 Header/Footer 共享组件
- [ ] GCP 部署流水线配置完成
- [ ] 准备好开始第 1 页

### 第 1 页：首頁

**您的任务**：
- [ ] 在 Figma 中完成首页设计
- [ ] 确保组件清晰标记
- [ ] 告诉我设计完成

**我的任务**：
- [ ] 生成所有 React 组件
- [ ] 测试本地显示
- [ ] 部署到 GCP
- [ ] 验证上线

### 第 2-11 页：重复流程

每页：Figma 设计 → 我生成代码 → 自动部署 → 验证完成

---

## ✅ 成功标准

### 每页完成时
```
✅ Figma 设计完整
✅ React 组件生成正确
✅ 本地显示无误
✅ GCP 上线成功
✅ 移动端适配正常
✅ 无控制台错误
```

### 11 页全部完成时
```
✅ 所有 11 个页面上线
✅ Header/Footer 在所有页面一致
✅ 导航链接全部正常
✅ SEO meta tags 完整
✅ 性能测试通过
✅ 完全可用的 Headless 前端
```

---

## 💡 关键优势

```
对比原方案：
┌─────────────────────────────────────┐
│ 原方案（Make 自动化）                 │
│ - 复杂设置                          │
│ - 等待整个工作流完成                  │
│ - 难以调整                          │
└─────────────────────────────────────┘

这个新方案（Figma → Roo Code → GCP）
┌─────────────────────────────────────┐
│ - 简单直接                          │
│ - 每页完成即可验证                    │
│ - 灵活快速调整                      │
│ - Header/Footer 一次性复用          │
│ - 自动部署无需人工干预                │
│ - 快速反馈循环                      │
└─────────────────────────────────────┘
```

---

## 📞 准备好开始？

当您准备好：
1. 在 Figma 中完成第 1 页（首頁）设计
2. 告诉我"第 1 页设计完成"
3. 我立即生成代码并部署

**建议**：第 1 页完成后，您可以同时继续设计第 2-3 页，我在生成其他页面代码。这样可以实现完全的并行化。

